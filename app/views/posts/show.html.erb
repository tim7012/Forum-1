<p>Title: <%= @post.title%></p>
<p><%= @post.content%></p>

<p>Category:
  <% @post.categorys.each do |g| %>
    <%= g.name %>
  <% end %>
</p>

<%= link_to "New Comment", new_post_comments_path(@post)%>
<p>Total comments:<%= @post.comments.count%></p>

<table>
  <% @post.comments.each do |c|%>
    <tr>

      <td><%= c.content %></td>

    </tr>
  <%end%>
</table>





<%= link_to "Back to Index", posts_path %>

